==============
plone template
==============

Use paster::

    >>> paster('create -t plone2_theme plone.example --no-interactive')
    paster create -t plone2_theme plone.example --no-interactive
    ...

Let's check the content::

    >>> package_dir = 'plone.example'
    >>> ls(package_dir)
    Extensions
    HISTORY.txt
    README.txt
    __init__.py
    config.py
    skins
    tests
    version.txt
